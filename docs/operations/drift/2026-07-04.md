# Seialz — Drift Report (banco vivo vs repo)

> Auditoria de 2026-07-04 cruzando banco de produção (`qvmtzfvkhkhkhdpclzua`) com o snapshot do repo (Lovable).
> **Este arquivo é temporário**: cada item vira issue/fix e sai da lista. Meta: arquivo vazio.

## P0 — corrigir esta semana

### 1. Auditoria DUPLICADA em contacts, opportunities e tasks
**Evidência:** cada tabela tem 3 triggers granulares (`audit_X_insert/update/delete`) + 1 combinada (`X_audit_trigger`, INS/DEL/UPD), todas → `audit_log_trigger`. Cada operação grava em `audit_logs` **duas vezes**.
**Impacto:** `audit_logs` = 292K linhas / **463 MB** (maior objeto do banco); write amplification no hot path; ~50% da tabela é lixo duplicado.
**Fix:** dropar as 3 granulares de cada tabela (manter a combinada), via migration. Depois avaliar expurgo dos duplicados históricos e retenção (particionar ou job de cleanup).
**Progresso 2026-07-05:** estado confirmado no banco vivo (12 triggers, 4 por tabela); origem identificada — duas migrations do repo de 2025-11-30 com ~23min de diferença criaram as duas famílias. SQL corretivo idempotente pronto para revisão em [`../proposals/2026-07-05-audit-trigger-dedup.sql`](../proposals/2026-07-05-audit-trigger-dedup.sql) (fora de `supabase/migrations/` para não auto-aplicar no push). **Pendente:** revisão + janela para aplicar; fase 2 (expurgo histórico + vacuum) separada.
**Status:** [~] parcial — proposta pronta; aplicação pendente

### 2. Functions deployadas FORA do repo
**Evidência:** `twilio-message-debug`, `meta-capi-raw-test`, `marketing-campaign-enrich` têm entrypoint `source/index.ts` (deploy ad-hoc via dashboard) em vez de `source/supabase/functions/...` (deploy via repo), versão parada.
**Impacto crítico:** `marketing-campaign-enrich` é chamada por **cron a cada 6h e por trigger** (`fn_marketing_campaign_enrich_async`) — código de produção sem versionamento.
**Fix:** baixar o código deployado, commitar no repo em `supabase/functions/`, redeployar via pipeline. `twilio-message-debug` e `meta-capi-raw-test`: avaliar exclusão (parecem debug).
**Progresso 2026-07-05:** código das 3 recuperado via API read-only. `marketing-campaign-enrich` commitada em `supabase/functions/` (com entrada no `config.toml`); as 2 de debug arquivadas em [`../shadow-functions/`](../shadow-functions/README.md) com recomendação de remoção futura (⚠️ `meta-capi-raw-test` tem auth fraca — só checa prefixo `Bearer`). **Pendente:** redeploy da enrich via pipeline (push = deploy, requer aprovação) e remoção das 2 debug do dashboard em janela aprovada. Nota: contagem real deployada em 2026-07-05 é **93** functions (snapshot de 2026-07-04 dizia 88).
**Status:** [~] parcial — código no repo; deploy/remoção pendentes

## P1 — próximas 2 semanas

### 3. `scheduled-messages-cron` órfã (VERIFICAR)
**Evidência:** function deployada (v275), mas nenhum dos 15 jobs do pg_cron a invoca.
**Risco:** se o disparo não vem de fora, mensagens agendadas do CRM não são enviadas silenciosamente (tabela `scheduled_messages` tem 3 linhas — verificar se são recentes e não enviadas).
**Fix:** confirmar origem do disparo; se órfã, recriar o cron job ou aposentar a feature explicitamente.
**Progresso 2026-07-05:** órfã CONFIRMADA no banco vivo (nenhum dos 15 jobs a invoca). As 3 linhas de `scheduled_messages` estão `pending` desde jan/2026 com `scheduled_at` no passado — a feature falha silenciosamente (tool `schedule_follow_up` do agente grava e nada envia). ⚠️ Recriar o cron sem antes cancelar essas 3 linhas as enviaria 6 meses atrasadas. Proposta com Passo 0 obrigatório + opções A (reativar) / B (aposentar) em [`../proposals/2026-07-05-scheduled-messages-cron.sql`](../proposals/2026-07-05-scheduled-messages-cron.sql). **Pendente:** decisão de produto (A ou B).
**Status:** [~] parcial — investigado; decisão de produto pendente

### 4. Migrations: repo (261) ≠ banco (184)
**Evidência:** `supabase_migrations.schema_migrations` = 184 aplicadas; repo tem 261 arquivos.
**Hipóteses:** squash histórico, migrations de branch nunca aplicadas, ou mudanças manuais paralelas.
**Fix:** rodar `supabase db diff` contra produção para gerar o diff real; alinhar; a partir daí, regra de ouro do OPERATIONS.md (mudança manual → migration no mesmo dia).
**Status:** [ ] pendente

### 5. verify_jwt=false em ~todas as 88 functions (incl. admin-impersonate)
**Evidência:** `list_edge_functions` mostra `verify_jwt: false` generalizado.
**Contexto:** pode ser intencional (auth própria em `_shared/auth.ts` — webhooks externos precisam de false). Mas exige confirmação function por function, especialmente `admin-impersonate*`, `create-user`, `byok-*`.
**Fix:** matriz function × mecanismo de auth (JWT próprio / assinatura de webhook / token interno do Vault / aberta) documentada em `platform/TENANCY-SECURITY.md`. Qualquer "aberta" sem justificativa = fechar.
**Status:** [ ] pendente

## P2 — housekeeping

### 6. 8 tabelas de backfill/backup soltas
`backup_meta_backfill_2026_05_28_contacts` (0), `deals?`→`opportunities_status_backup_20260512` (1.559), `messages_endpoint_backfill_2b` (92K!), `message_threads_business_context_backfill` (17K), `..._backfill_20260703` (308), `..._backfill_null_20260703` (38), `message_threads_primary_endpoint_backfill` (3.9K).
**Fix:** confirmar que os backfills foram concluídos → dropar via migration (as duas de 2026-07-03 aguardar 30 dias). Padrão futuro: backfill em schema `_scratch`, não em `public`.
**Status:** [ ] pendente

### 7. Duplicações menores a consolidar
`rpc_list_message_threads` com 2 overloads; `validate_message_analysis_v2` + `v21` (duas triggers na mesma tabela); `assign_round_robin` com 2 assinaturas (ok se intencional — documentar).
**Status:** [ ] pendente

### 8. Org hardcoded em trigger
`parse_lead_source_marker_from_message` tem o UUID da Central Trabalhista hardcoded ("escopo v1"). Funciona, mas é regra multi-tenant embutida em código — mover para flag/config por org quando o segundo tenant precisar.
**Status:** [ ] registrado (não urgente)
