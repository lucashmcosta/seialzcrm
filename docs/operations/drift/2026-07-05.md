# Seialz — Drift Report (2026-07-05)

> Complementa o drift #4 de [`2026-07-04.md`](2026-07-04.md) (migrations repo ≠ banco).
> **Este arquivo é temporário**: cada item vira issue/fix e sai da lista.

## 9. Migrations de 2026-04-21 aplicadas em prod mas não registradas

**Evidência:** verificação no banco vivo (2026-07-05) confirmou que todo o conteúdo de
`supabase/migrations/20260421000000_fix_round_robin_fk_violation.sql` e
`supabase/migrations/20260421000001_add_ad_referral_ctwa_clid.sql` **já está aplicado em produção**
(trigger `contacts_round_robin_audit` existe; `trg_contacts_round_robin` já é a versão corrigida sem
INSERT em `activities`; coluna `contacts.ad_referral_ctwa_clid` e índice parcial existem) — porém as
versões `20260421000000` e `20260421000001` **não constam** em `supabase_migrations.schema_migrations`.

**Decisão (2026-07-05):** os dois arquivos foram commitados como **registro histórico do estado real
de produção**. Não re-executar (ambos são idempotentes, mas não há o que aplicar).

**Fix pendente:** na reconciliação do drift #4, registrar as versões no histórico sem executar:

```sh
supabase migration repair --status applied 20260421000000 20260421000001
```

Nenhuma alteração em `schema_migrations` foi feita agora.

**Status:** [ ] pendente (aguarda reconciliação do drift #4)
