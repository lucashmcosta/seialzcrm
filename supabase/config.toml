project_id = "qvmtzfvkhkhkhdpclzua"

[functions.meta-whatsapp-webhook]
verify_jwt = false

[functions.meta-whatsapp-send]
verify_jwt = false

[functions.meta-whatsapp-templates-sync]
verify_jwt = false

[functions.meta-wa-diagnose]
verify_jwt = false

# Auth propria via _shared/auth.ts (validateServiceRoleAuth) — chamada por
# pg_cron/pg_net com service_role. Igual ao estado deployado (drift P0 #2).
[functions.marketing-campaign-enrich]
verify_jwt = false

# Preserva o estado atual de producao (verify_jwt=false) no deploy via CLI.
# Auth real: EDGE_AUTH (validateCallerAuth, modo log) / assinatura Twilio.
# CLI default seria true — quebraria o webhook Twilio (sem JWT) no gateway.
[functions.twilio-whatsapp-send]
verify_jwt = false

[functions.ai-agent-respond]
verify_jwt = false

[functions.twilio-webhook]
verify_jwt = false

# Webchat v1: chamadas por visitante anonimo (widget key publica + session token).
# Sem JWT de usuario/service_role; auth propria via _shared/webchat.ts (widget
# key + Origin allowlist). Precisa de verify_jwt=false (CLI default seria true).
[functions.webchat-config]
verify_jwt = false

[functions.webchat-message]
verify_jwt = false

[functions.webchat-messages]
verify_jwt = false

[functions.webchat-heartbeat]
verify_jwt = false
